#include <stdio.h>

// Custom function that has two integer parameters (a,b)
// and returns an integer named c_squared
int Pythagorean_Theorem (int a, int b)
{
    int c_squared = (a*a) + (b*b);
    return c_squared;
}
